<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lts.web.repository.mapper.JVMInfoRepo">

    <insert id="insert" parameterType="com.lts.web.repository.domain.JVMInfoDataPo" flushCache="true">
        <![CDATA[
        INSERT INTO `lts_admin_jvm_info`
        (`gmt_created`,
        `identity`,
        `timestamp`,
        `node_type`,
        `node_group`,
        `jvm_info`)
        VALUES
      (
        #{gmtCreated},
        #{identity},
        #{timestamp},
        #{nodeType},
        #{nodeGroup},
        #{jvmInfo})
        ]]>
    </insert>

    <select id="select" parameterType="com.lts.web.request.JVMDataGetRequest"
            resultType="JVMInfoDataPo">
        SELECT
         id,
         gmt_created as gmtCreated,
        `identity`,
        `timestamp`,
        `node_type` as nodeType,
        `node_group` as nodeGroup,
        `jvm_info` as jvmInfo
         FROM
        `lts_admin_jvm_info`
        WHERE `identity` = #{identity} ORDER BY `gmt_created` DESC LIMIT 0,1
    </select>

</mapper>